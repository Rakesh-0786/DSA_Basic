#include<bits/stdc++.h>
using namespace std;
vector<vector<int>> pascalTraingle(int n){
    vector<vector<int>> pascal(n);
    for(int i=0;i<n;i++){
        pascal[i].resize(n);
        for
    }
}
int main(){
    return 0;
}